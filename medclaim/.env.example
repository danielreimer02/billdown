# Copy this to .env and fill in real values for deploy profile
# Usage: docker compose --profile deploy up -d --build

SECRET_KEY=change-me-to-a-real-secret
POSTGRES_PASSWORD=change-me-to-a-real-password
